<p>this is the owner index</p>
<p><%= current_user.email %></p>
<p>Owner: <%= current_user.owner? %></p>
<% @flats.each do |flat| %>
  <p><%= flat.name %></p>
<% end %>



<div class="container my-4">
<h1 class="text-center my-4">Welcome <%= @user.email %></h1>

<h2>Here are your Flats</h2>
<div class="row ">
  <%  @flats.each do |flat| %>
    <div class="card-trip col-sm-11 col-md-6 col-lg-4">
      <img src="<%= flat.poster_url %>" />
      <div class="card-trip-infos mb-3">
          <h2><%= flat.name %></h2>
          <p><%= flat.description %></p>
      </div>
    </div>
  <% end %>
</div>

</div>
